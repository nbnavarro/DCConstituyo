<div class="form-container">
	<% if (!currentUser){ %>
		<div class="card">
			<h2>Registrarse como votante</h2>
			<%- include('_form') %>
		</div>
		
	<% } else { %>
		<h2>No deberías estar aquí 🤨</h2>
	<% } %>
</div>
