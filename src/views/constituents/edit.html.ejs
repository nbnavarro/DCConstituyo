<div class="form-container">
	<% if (constituent.id == idUser && 'constituent' == userType){%>
		<div class="card">
			<h2>Editar perfil</h2>
			<%- include('_form') %>
		</div>	
	<% } else { %>
		<h2>No deberías estar aquí</h2>
	<% } %>
</div>
